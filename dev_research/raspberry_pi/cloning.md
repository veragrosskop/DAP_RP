# Cloning a Repo

On the raspberry pi we have we use the `~/projects` directory for any python projects. 
To clone a repo follow these steps:

## Navigate to folder

`cd ~/projects`

## Clone repo

`git clone <path_to_repo>`
